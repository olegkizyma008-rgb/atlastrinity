# АТЛАС Трініті (AtlasTrinity)

## 🚀 Про проект
**АТЛАС Трініті** — це інтелектуальна мультиагентна екосистема, призначена для автономного керування macOS, професійної розробки ПЗ та глибокої автоматизації процесів. Проект переосмислює взаємодію людини та ШІ, створюючи повноцінного "цифрового колегу".

---

## 👁️ Візуальний Інтелект та Мультимоніторність
Однією з унікальних характеристик **АТЛАС Трініті** є просунута система візуального аудиту:
- **Quartz & AppleScript**: Гріша не просто робить знімок екрана, він взаємодіє з macOS API для ідентифікації активних вікон.
- **Multi-Monitor Stitching**: Система здатна зшивати зображення з кількох моніторів у єдиний контекстний панорамний знімок для GPT-4o Vision.
- **Focus Analysis**: Гріша фокусується на конкретному додатку, з яким працює Тетяна, що мінімізує "галюцинації" ШІ.

---

## 🛡️ Безпека та Етичні Бар'єри (Guardrails)
Ми інтегрували багаторівневу систему безпеки:
- **Command Blocklist**: Жорсткий список заборонених деструктивних команд (наприклад, несанкціоноване видалення кореневих папок).
- **Grisha Security Audit**: Кожен крок плану проходить через Грішу як незалежного аудитора. Якщо він бачить ризик — виконання блокується до підтвердження користувачем.
- **Isolated Execution**: Підтримка роботи в Docker-контейнерах для безпечного тестування коду.

---

## 🎙️ Людино-Машинний Інтерфейс (Voice & UI)
**АТЛАС Трініті** має "людське обличчя":
- **STT/TTS**: Інтеграція з OpenAI Whisper для розпізнавання голосу та якісний синтез мовлення (голоси Дмитра та Микити).
- **Bilingual Support**: Система вільно оперує українською та англійською мовами, адаптуючи технічну термінологію та розмовний стиль.
- **Real-time Pub/Sub UI**: Завдяки Redis, стан "мозку" миттєво відображається в інтерфейсі користувача.

---

[... попередні розділи: Архітектура, Агенти, Як працює програма, GraphChain, MCP, Пам'ять, Мета-логіка ...]

## 💾 Технологічний стек
- **Core**: Python 3.12 (Asyncio)
- **Database**: PostgreSQL 16 (SQLAlchemy 2.0) + Redis 7
- **Vector Store**: ChromaDB (Semantic Memory)
- **Models**: GPT-4o, GPT-4o-mini, Raptor-Mini
- **Integration**: MCP Servers, AppleScript, Quartz API
- **Audio**: OpenAI Whisper (STT) + macOS Speech (TTS)

---

## 🧠 Архітектура Різонінгу (Логічного мислення)

Система використовує **Multi-Agent Reasoning Loop**, де кожен агент має власну спеціалізацію та рівень доступу до даних.

### 1. Atlas (The Strategist)
*   **Роль**: Глобальне планування, архітектурні рішення, вивчення досвіду.
*   **Механізм мислення**:
    *   **Sequential Thinking**: Для складних задач використовує покрокове розкладання проблеми на атомарні дії.
    *   **Memory Retrieval**: Перед початком роботи шукає подібні завдання у векторній базі **ChromaDB**. Якщо знайдено успішний досвід (Golden Path), він використовується як шаблон.
    *   **Verification Gate**: Атлас проводить фінальний аудит виконаної роботи, виставляючи оцінку якості (Quality Score).

### 2. Tetyana (The Executor)
*   **Роль**: Написання коду, робота з терміналом, виконання інструментів.
*   **Механізм мислення**:
    *   **Contextual Awareness**: Постійно відстежує стан робочої директорії, активні файли та змінні середовища через `SharedContext`.
    *   **Recursive Recovery**: У разі помилки Тетяна аналізує причину, просить допомоги у Атласа та автоматично виправляє свої кроки.

### 3. Grisha (The Auditor)
*   **Роль**: Верифікація результатів через комп'ютерний зір (Vision) та безпека.
*   **Механізм мислення**:
    *   **Vision-Based Verification**: Робить скріншоти (з фокусом на активне вікно), аналізує їх через GPT-4o Vision, щоб підтвердити фактичне виконання завдання.
    *   **Security Filter**: Пропускає кожну команду Тетяни через фільтр безпеки, блокуючи деструктивні дії (наприклад, `rm -rf /`).

---

## 🛠 Можливості Агентів

| Агент | Інструменти (Tools) | Основні функції |
| :--- | :--- | :--- |
| **Atlas** | `sequential-thinking`, `mem-search` | Створення планів, допомога при помилках, оцінка якості. |
| **Tetyana** | `terminal`, `filesystem`, `browser`, `docker` | Кодування, деплой, запуск команд, маніпуляція файлами. |
| **Grisha** | `screenshot`, `security-check`, `file-verify` | Візуальна перевірка, аудит логів, безпека системи. |

---

## 🏗 Як працює програма

Програма працює як циклічний оркестратор:

1.  **Прийом запиту**: Користувач вводить команду.
2.  **Аналіз контексту**: Атлас аналізує запит, перевіряє історію в **PostgreSQL** та пам'ять у **ChromaDB**.
3.  **Планування**: Атлас створює покроковий план у форматі JSON.
4.  **Виконання**: Тетяна бере кожен крок і виконує його за допомогою відповідного **MCP-сервера**.
5.  **Перевірка**: Гріша перевіряє результат кожного кроку. Якщо результат не підтверджено — Тетяна йде на повтор або Атлас коригує план.
6.  **Knowledge Graph**: Всі вузли (Файли, Інструменти, Завдання) та зв'язки між ними автоматично записуються в граф для подальшого аналізу.
7.  **Консолідація**: Вночі або під час простою система аналізує всі помилки та успіхи за день, дистилюючи їх у нові правила пам'яті.

---

## � GraphChain (Knowledge Graph)
**GraphChain** — це структурована пам'ять системи, реалізована через PostgreSQL. Вона дозволяє ШІ не просто "пам'ятати текст", а розуміти взаємозв'язки.

*   **Типи вузлів**:
    *   `TASK`: Цілі та підзадачі.
    *   `FILE`: Документи, код, конфігурації, з якими працювала система.
    *   `TOOL`: MCP-інструменти (Terminal, Filesystem тощо).
    *   `USER`: Користувач-ініціатор.
*   **Зв'язки**: Система автоматично будує ребра на кшталт `TASK -> USES -> TOOL` або `TASK -> MODIFIED -> FILE`. Це створює "цифровий відбиток" діяльності ШІ, який можна візуалізувати за допомогою інструменту `graph.generate_mermaid`.

---

## 🔌 Екосистема MCP (Model Context Protocol)
MCP — це "нервова система" AtlasTrinity. Вона дозволяє агентам безшовно взаємодіяти з macOS та зовнішніми API.

*   **Модульність**: Кожен інструмент (термінал, файлова система, пошук) — це окремий ізольований сервер.
*   **Динамічна підстановка**: Система автоматично замінює змінні середовища (напериклад, `${PROJECT_ROOT}`) у конфігураціях, що робить її переносимою між різними машинами.
*   **Безпека**: Всі виклики інструментів проходять логування та аудит Гріші.

---

## 🧠 Система Пам'яті (Memory Hierarchy)

| Рівень | Технологія | Призначення |
| :--- | :--- | :--- |
| **Короткострокова** | `SharedContext` + Redis | Поточний стан, відкриті файли, CWD, остання успішна операція. |
| **Структурована** | PostgreSQL | Історія завдань, кроків, GraphChain, логів. Чисті дані для консолідації. |
| **Семантична** | ChromaDB (Vector Store) | "Досвід". Зберігає абстрактні стратегії та уроки для пошуку за подібністю. |

---

## 📡 Реальний час (Events & Pub/Sub)
AtlasTrinity використовує **Redis Pub/Sub** для зв'язку "Мозку" (Python) та інтерфейсу (UI).
- **Events**: Кожна подія (початок планування, виконання кроку, завершення задачі) транслюється в реальному часі.
- **Live Logs**: Це дозволяє створювати динамічні дашборди, де видно кроки Тетяни та думки Атласа миттєво.

---

## 🧬 Глибинні Механізми (Meta-Logic)

### 1. Meta-Planning (Планування Плану)
Атлас не просто створює список кроків. Він займається **мета-плануванням**:
- **Аналіз складності**: Оцінює, чи потребує задача глибокого мислення (Sequential Thinking).
- **Вибір стратегії**: На основі векторного пошуку вибирає, чи йти за перевіреним шаблоном, чи синтезувати новий підхід.
- **Делегування з прогнозом**: Кожен крок містить очікуваний результат, що дозволяє системі зрозуміти відхилення ще до завершення всього завдання.

### 2. Рекурсивні Петлі Відновлення (Recursive Self-Correction)
Тетяна працює в режимі **рекурсивного циклу**:
- Якщо крок провалюється, викликається функція `help_tetyana`.
- Це породжує підзадачу (subtask), яка виконується за тими самими правилами, що й основна.
- Кількість рівнів вкладеності практично необмежена, що дозволяє AtlasTrinity "розплутувати" помилки будь-якої глибини, доки не буде знайдено робоче рішення.

### 3. Семантична "Інтуїція" (Vectors & Semantics)
Завдяки **ChromaDB** та векторним ембеддингам (OpenAI v3-small/large), система має подобу інтуїції:
- **Semantic Retrieval**: Коли ви ставите нове завдання, Атлас "відчуває" схожість з минулим не за словами, а за змістом (семантично).
- **Data Distillation**: Під час нічної консолідації сирі логи перетворюються на абстрактні вектори "Уроків". Це дозволяє системі уникати однакових помилок у майбутньому.

---

## 🌟 Потенціал Системи (Full Potential)

AtlasTrinity — це не просто чат-бот, це **цифровий співробітник** з потенціалом:
- **Автономна розробка**: Створення проектів "під ключ" — від ініціалізації репозиторію до написання коду, тестів та деплою в Docker.
- **Кібер-асистент**: Повний контроль над macOS — управління календарем, поштою, файлами та інтерфейсом через комп'ютерний зір.
- **Самонавчання**: З кожним виконаним завданням GraphChain стає щільнішим, а база стратегій — багатшою. Система стає розумнішою саме у вашому специфічному контексті.
- **Прозорість**: Завдяки візуалізації графів та Pub/Sub логам, ви завжди бачите "внутрішню кухню" ШІ — його сумніви, логічні гілки та методи перевірки.

---

## 💾 Технологічний стек
- **Core**: Python 3.12 (Asyncio)
- **Database**: PostgreSQL 16 (SQLAlchemy 2.0) + Redis 7
- **Vector Store**: ChromaDB (для семантичного пошуку)
- **Models**: GPT-4o, GPT-4o-mini, Raptor-Mini (через Copilot SDK)
- **Integration**: MCP Servers, AppleScript (Automation), Quartz (UI Capture)
- **DevOps**: Docker (для ізольованих середовищ виконання)
